# EMR python 3.6 tests

Adds the aws-java-sdk and hadoop-aws jars to the base jupyter pyspark image and creates a python 3.6 conda environment called "test"

Suitable for running unit tests requiring pyspark.
